import * as NS from '../../namespace';
import initial from '../initial';

function usersReducer(state: NS.IReduxState, { type, payload }: any) {
  switch (type) {
    case '':

    default:
      return state;
  }
}

export default usersReducer;
